<?xml version="1.0" encoding="utf-8"?>
<!--
  EVEN GIRL — ağ güvenliği yapılandırması.

  İKİNCİ SAVUNMA KATMANI: OkHttp CertificatePinner (PinnedHttpClient.kt) zaten
  SPKI pinning yapar. Bu dosya bağımsız bir katman ekler:

  1. cleartextTrafficPermitted="false" — düz HTTP tamamen yasak. Yanlışlıkla
     http:// bir URL kullanılırsa istek OS seviyesinde reddedilir.
  2. <trust-anchors> yalnızca "system" içerir — KULLANICI tarafından eklenen
     CA'lar (Burp/Charles/mitmproxy kökleri) güvenilmez. Android 7+'da bu
     zaten varsayılandır, ama açıkça yazmak yanlışlıkla debug ayarının
     release'e sızmasını engeller.
  3. <pin-set> — OkHttp devre dışı kalsa bile (ör. üçüncü parti bir SDK kendi
     HttpURLConnection'ını kullanırsa) platform seviyesinde pin uygulanır.

  DİKKAT: pin-set expiration'ı geçtiğinde platform pinning'i SESSİZCE devre
  dışı bırakır (fail-open). Bu tarih, yedek pin rotasyonunun son tarihidir;
  CI her sürümde tarihin 90 günden yakın olmadığını kontrol eder.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">api.armanalabs.com</domain>
        <pin-set expiration="2027-06-01">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">crash.armanalabs.com</domain>
        <pin-set expiration="2027-06-01">
            <pin digest="SHA-256">CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC=</pin>
            <pin digest="SHA-256">DDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDDD=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
